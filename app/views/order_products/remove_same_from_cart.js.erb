$("#order_product_item_<%= @order_product.id %>").remove();
$("#cart_total_price").text('<%= number_to_currency(@cart.total_price,
                                        unit: 'грн.',
                                        format: '%n %u',
                                        separator: ',') %>');